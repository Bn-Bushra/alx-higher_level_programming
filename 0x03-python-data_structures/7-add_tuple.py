#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    if tuple_a[0] or tuple_a[1]:
        0
    x, y = tuple_a
    h, i = tuple_b
    if x or y or h or i < 2:
        x or y or h or i == 0
    else:
        pass
    a = x+h
    b = y+i
    return (a,b)

# tuple_a = (1, 89)
# tuple_b = (88, 11)
# print(add_tuple(tuple_a, tuple_b))