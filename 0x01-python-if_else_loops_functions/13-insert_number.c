#include "lists.h"
/***
 * insert_node - inserts a number into a sorted singly linked list
 * 
*/
listint_t *insert_node(listint_t **head, int nubmer){
  add_nodeint_end(&head, number);
 return NULL
}